# === SENSITIVE DATA — never commit ===
03.DATA/raw/
04.REPORTS/outputs/
# === RADAR HIST — local archive, not in git ===
05.DOCS/HIST/

# === Python ===
__pycache__/
*.py[cod]
*.pyo
.env
.venv
venv/
*.egg-info/

# === OS ===
.DS_Store
Thumbs.db
desktop.ini

# === VS Code ===
.vscode/settings.json
.vscode/launch.json

# === Excel temp files ===
~$*.xlsx
~$*.xlsm

# === SQLite (future phases) ===
*.db
*.db-shm
*.db-wal
99.TEMP/
05.DOCS/RADAR.*.txt
03.DATA/staging/*.csv
!03.DATA/staging/.gitkeep

# GENERATED_ARTIFACTS_POLICY_CR_TEMP_0011
# Generated local-only artifacts:
# - 99.TEMP/ contains one-shots, evidence, RADAR_ACTIVE outputs, upload context.
# - 05.DOCS/RADAR.*.txt are legacy generated RADAR outputs; local-only.
# - 05.DOCS/HIST/ is local-only.
# - 03.DATA/staging/*.csv is derived from sensitive raw data; local-only unless sanitized.
98.IA.History/

# AI_HISTORY_POLICY_CR_TEMP_0012
# AI conversation/history exports are local-only by default.
# They may contain sensitive context and should only be committed if sanitized and explicitly approved.
98.IA.History/
